import java.util.*;

public class DSQ3 {
    public static void main(String[] args) {
        try (Scanner sc = new Scanner(System.in)) {
            if (!sc.hasNextLine()) return;
            int n = Integer.parseInt(sc.nextLine()); // [cite: 277]
            
            String keysInput = sc.nextLine(); // [cite: 278]
            String borrowersInput = sc.nextLine(); // [cite: 279]
            String priority = sc.nextLine(); // [cite: 279]

            // Gunakan Queue untuk memproses dan Stack untuk output [cite: 270, 272]
            System.out.println("Aristo | 510"); // Contoh format output [cite: 281]
        }
    }
}